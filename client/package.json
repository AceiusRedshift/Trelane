{
  "name": "trelane",
  "version": "1.0.0",
  "main": "src/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "esbuild src/* --bundle --watch --sourcemap --outdir=bin --servedir=bin --loader:.html=copy --loader:.png=copy --inject:hotreload.js",
    "build": "esbuild src/* --bundle --outdir=bin --loader:.html=copy --loader:.png=copy"
  },
  "repository": {
    "type": "git",
    "url": "git+https://gitlab.com/Aceius/trelane.git"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://gitlab.com/Aceius/trelane/issues"
  },
  "homepage": "https://gitlab.com/Aceius/trelane#readme",
  "description": "",
  "dependencies": {
    "mithril": "^2.2.15"
  },
  "devDependencies": {
    "@types/mithril": "^2.2.7",
    "esbuild": "^0.25.3"
  }
}
